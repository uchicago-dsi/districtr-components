import { UnitConfigProps } from '../Districtr/Districtr.types';
export declare const generateUnits: (unitsConfig: UnitConfigProps, unitCount: number, totalMembers: number, unitName: string, unitNamePlural: string, unitType: 'single' | 'multi' | 'multi-varying', sumPopulation: number) => UnitConfigProps;
